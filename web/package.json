{
  "name": "orber-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "wasm:install": "command -v wasm-pack >/dev/null 2>&1 || cargo install wasm-pack --locked",
    "wasm:build": "cd .. && wasm-pack build crates/wasm --target web --out-dir ../../web/src/wasm --release",
    "dev": "npm run wasm:build && astro dev",
    "build": "npm run wasm:build && astro build",
    "build:cf": "npm run wasm:install && npm run build",
    "preview": "astro preview",
    "deploy": "wrangler pages deploy dist"
  },
  "dependencies": {
    "@astrojs/solid-js": "^4.4.0",
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^4.16.0",
    "solid-js": "^1.9.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
