{
  "name": "orber-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "wasm:ensure-rust": "which rustup || (unset RUSTUP_VERSION && curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable && . $HOME/.cargo/env && rustup target add wasm32-unknown-unknown)",
    "wasm:install": ". $HOME/.cargo/env 2>/dev/null; which wasm-pack || cargo install wasm-pack --locked",
    "wasm:build": "cd .. && wasm-pack build crates/wasm --target web --out-dir ../../web/src/wasm --release",
    "gen:qr": "node scripts/gen-qr.mjs",
    "stamp:sw": "node -e \"const f=require('fs'),p='dist/sw.js',d=new Date(Date.now()+9*36e5).toISOString().slice(0,10);f.writeFileSync(p,f.readFileSync(p,'utf-8').replaceAll('__BUILD_DATE__',d))\"",
    "dev": "npm run wasm:build && npm run gen:qr && astro dev",
    "build": "npm run wasm:build && npm run gen:qr && astro build && npm run stamp:sw",
    "build:cf": "npm run wasm:ensure-rust && . $HOME/.cargo/env 2>/dev/null; npm run wasm:install && npm run wasm:build && npm run gen:qr && astro build && npm run stamp:sw",
    "preview": "astro preview",
    "deploy": "wrangler pages deploy dist"
  },
  "dependencies": {
    "@astrojs/solid-js": "^4.4.0",
    "@astrojs/tailwind": "^5.1.0",
    "astro": "^4.16.0",
    "jszip": "^3.10.1",
    "mp4-muxer": "^5.2.2",
    "qrcode": "^1.5.4",
    "solid-js": "^1.9.0",
    "tailwindcss": "^3.4.0",
    "webm-muxer": "^5.1.4"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.6",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
